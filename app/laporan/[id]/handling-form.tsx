'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';
export function HandlingForm({reportId,initialStatus}:{reportId:string;initialStatus:string}){const router=useRouter(),[busy,setBusy]=useState(false),[info,setInfo]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setInfo('');const d=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/laporan/handling',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({laporanId:reportId,status:d.status,waktuPenanganan:d.waktuPenanganan,petugas:d.petugas,dokumentasi:d.dokumentasi,catatan:d.catatan})});const j=await r.json();setInfo(j.message||j.error);setBusy(false);if(r.ok)router.refresh()}return <form onSubmit={submit} className="rounded-2xl border border-indigo-100 bg-indigo-50 p-6"><h2 className="font-bold text-indigo-950">Perbarui Penanganan</h2><p className="mt-1 text-sm text-indigo-700">Setiap pembaruan disimpan sebagai riwayat baru.</p><div className="mt-4 space-y-3"><select name="status" defaultValue={initialStatus} className="w-full rounded-xl border border-indigo-200 p-3 text-sm">{['Belum Ditangani','Dalam Proses','Selesai'].map(x=><option key={x}>{x}</option>)}</select><input name="waktuPenanganan" type="datetime-local" className="w-full rounded-xl border border-indigo-200 p-3 text-sm"/><input name="petugas" placeholder="Petugas yang menangani" className="w-full rounded-xl border border-indigo-200 p-3 text-sm"/><textarea name="dokumentasi" placeholder="Tautan dokumentasi (satu per baris)" className="min-h-20 w-full rounded-xl border border-indigo-200 p-3 text-sm"/><textarea name="catatan" placeholder="Catatan penanganan" className="min-h-20 w-full rounded-xl border border-indigo-200 p-3 text-sm"/></div>{info&&<p className="mt-3 rounded-lg bg-white p-3 text-sm text-indigo-800">{info}</p>}<button disabled={busy} className="mt-4 rounded-xl bg-indigo-700 px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-60">{busy?'Menyimpan…':'Simpan Pembaruan'}</button></form>}
