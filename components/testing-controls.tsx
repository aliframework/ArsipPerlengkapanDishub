'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation';
export function TestingControls(){const router=useRouter();const [busy,setBusy]=useState(''),[message,setMessage]=useState('');async function run(method:'POST'|'DELETE'){if(method==='DELETE'&&!confirm('Hapus semua laporan dummy? Data laporan asli tidak akan dihapus.'))return;setBusy(method);setMessage('');const r=await fetch('/api/testing/dummy',{method});const data=await r.json();setMessage(data.message||data.error||'Selesai');setBusy('');if(r.ok)router.refresh()}return <section className="rounded-2xl border border-dashed border-amber-300 bg-amber-50 p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-bold tracking-[.14em] text-amber-700">PENGUJIAN DEVELOPMENT</p><h3 className="mt-1 font-bold text-amber-950">Data Dummy Sementara</h3><p className="mt-1 text-sm text-amber-800">Membuat tiga laporan dummy—satu untuk setiap kecamatan. Hanya laporan berawalan DISHUB-DEMO yang dapat dihapus.</p></div><div className="flex gap-2"><button disabled={!!busy} onClick={()=>run('POST')} className="rounded-lg bg-amber-500 px-3 py-2 text-sm font-semibold text-white disabled:opacity-60">{busy==='POST'?'Menambah…':'Tambah Dummy'}</button><button disabled={!!busy} onClick={()=>run('DELETE')} className="rounded-lg border border-rose-200 bg-white px-3 py-2 text-sm font-semibold text-rose-700 disabled:opacity-60">{busy==='DELETE'?'Menghapus…':'Hapus Dummy'}</button></div></div>{message&&<p className="mt-3 rounded-lg bg-white px-3 py-2 text-sm text-slate-700">{message}</p>}</section>}
